import React from "react";
import './Options.css'

function Options({children}){
    return(
        <div className="options">
            
            {children}  
        </div>
    )
}
export {Options}
